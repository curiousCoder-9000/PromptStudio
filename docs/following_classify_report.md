# Following classify report (generated locally)

This file is **not** a personal data dump. Generate reports on your machine:

```powershell
py scripts/classify_following.py
```

Reports (`following_classify_report.json`, etc.) stay gitignored because they contain personal usernames, paths, and classification notes.
