# Following list (generated locally)

This file is **not** a personal data dump. Generate your own list on your machine:

```powershell
# Requires INSTAGRAM_SESSION_USER + Instaloader session in .env
py scripts/export_following_list.py
```

Output:

- `following_list.json` (gitignored) — machine-readable

See `following_list.example.json` for the expected JSON shape.
